% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/EstimatePseudoCell.R
\name{EstimatePseudoCell}
\alias{EstimatePseudoCell}
\title{Estimate Pseudo-cell Types with Elbow-based PC Selection}
\usage{
EstimatePseudoCell(
  data_mat,
  batch,
  ncl_min = 2,
  ncl_max = 10,
  seed = 1,
  r_random_cell = 0.01,
  n_random_cell = 1000,
  pheno = NULL,
  max_pcs_elbow = 50
)
}
\arguments{
\item{data_mat}{Numeric matrix, cells-by-genes.}

\item{batch}{Factor of batch labels.}

\item{ncl_min}{Minimum K to evaluate (default 2).}

\item{ncl_max}{Maximum K to evaluate (default 10).}

\item{seed}{Random seed (default 1).}

\item{r_random_cell}{Proportion of cells for initial clustering
(default 0.01).}

\item{n_random_cell}{Minimum cells for initial clustering
(default 1000).}

\item{pheno}{Optional phenotype covariate (default NULL).}

\item{max_pcs_elbow}{Maximum PCs for elbow search (default 50).}
}
\value{
A list with \code{cluster}, \code{n_pcs}, \code{history},
and \code{elbow_res}.
}
\description{
When cell-type labels are unavailable, infers pseudo-cell-type
clusters in three stages: (1) PCA with automatic elbow-based
dimensionality selection, (2) K-means initial clustering with
silhouette-based K selection, (3) iterative refinement via
orthogonal batch removal until BIC converges.
}
\examples{
\dontrun{
pseudo <- EstimatePseudoCell(expr_mat, batch = factor(meta$batch))
table(pseudo$cluster)
}
}
